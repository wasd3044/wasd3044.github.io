define(function (angular, main) {
    'use strict';
    blogApp.registerController('designCtrl', function ($scope, $stateParams, $state, $$http) {
        var vm = this;
        $scope.changeShowTitle(false);
    });
});